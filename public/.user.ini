; Applied by PHP-FPM / CGI when per-dir .user.ini is enabled (e.g. some Herd setups).
; If imports still fail around 2MB locally, raise post_max_size in Herd's php.ini.
memory_limit = 256M
post_max_size = 12M
upload_max_filesize = 12M
max_execution_time = 120
